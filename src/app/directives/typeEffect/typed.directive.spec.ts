import { TypedDirective } from './typed.directive';

describe('TypedDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedDirective();
    expect(directive).toBeTruthy();
  });
});
